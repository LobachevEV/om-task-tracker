import { describe, it, expect } from 'vitest';

describe('ganttStageGeometry (v1 suite removed in v2 taxonomy)', () => {
  it('is removed; v2 uses computeFeatureGeometry / computePhaseGeometry instead', () => {
    expect(true).toBe(true);
  });
});
