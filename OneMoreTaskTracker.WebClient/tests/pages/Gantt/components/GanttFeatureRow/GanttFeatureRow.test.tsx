import { describe, it, expect } from 'vitest';

describe('GanttFeatureRow (v1 suite removed in v2 taxonomy)', () => {
  it('is removed; replaced by track/gate/phase composition tests', () => {
    expect(true).toBe(true);
  });
});
