# Generator State

Tracks what each iteration changed so subsequent iterations can skip already-done work.

| Iter | Summary | Commit |
|---|---|---|
| 1 | Spec 02 (Features gRPC contract). Added `Protos/feature_state.proto` + 4 per-use-case protos (`CreateFeatureCommand`, `UpdateFeatureCommand`, `ListFeaturesQuery`, `GetFeatureQuery`) with `FeatureDto` duplicated in each (mirrors Tasks' TaskDto duplication). Enum ordinals mirror C# (CsApproving=0..LiveRelease=4), matching `task_state.proto` style. Added 4 handler stubs under `Features/{Create,Update,List,Get}/` throwing `RpcException(Unimplemented, "see spec 03"/"spec 04")`. Updated `OneMoreTaskTracker.Features.csproj` with 5 `<Protobuf>` items. Wired `app.MapGrpcService<…>()` for all 4 handlers in `Program.cs`. Added `HandlerRegistrationTests.cs` with 4 xUnit facts via direct handler construction. Build: clean. Tests: 6/6. **Evaluator score 8.8/10 → PASS (threshold 7.0)**. | `2a00939` |
| 2 | Spec 06 iter 1 — Frontend feature types + planApi client. Added `src/shared/types/feature.ts` (FeatureState, FEATURE_STATES, MiniTeamMember, AttachedTask, FeatureSummary, FeatureDetail, CreateFeaturePayload, UpdateFeaturePayload, FeatureScope; reuses `TaskState` via import). Extended `src/shared/api/schemas.ts` with `featureStateSchema`, `featureSummarySchema`, `featureSummaryListSchema`, `featureDetailSchema` (reuses existing `taskStateSchema` + `userRoleSchema`; empty-string → null on description; ISO-regex on dates). Added `src/shared/api/planApi.ts` with `listFeatures`/`getFeature`/`createFeature`/`updateFeature`/`attachTask`/`detachTask`, each Zod-parsed. Added `featureId: number` (required) to `CreateTaskPayload`; updated `tasksApi.test.ts` payloads and added `@ts-expect-error` for old shape (§310). TaskPage: introduced `inferredFeatureId: number \| null = null` placeholder + TODO for spec 06 §306; "Add" button now disables with `title={t('new.disabledNoFeature')}` when no feature context (used existing `tasks:new.disabledNoFeature` key instead of `tasks:addTask.disabledNoFeature` — already present in en/ru locales). New `planApi.test.ts` covers URL assembly, encoding, schema-rejection, and schema normalization. Build: clean. Tests: 215/215 across 27 files. | (uncommitted) |
| 3 | Spec 05 — Task↔Feature attachment handlers. New proto `Protos/AttachTaskCommand/attach_task_command_handler.proto` (service `TaskFeatureLinker` with `Attach`/`Detach`, messages per spec §§25–58, package `mr_helper.tasks`, csharp_namespace `OneMoreTaskTracker.Proto.Tasks.AttachTaskCommand`). Added `<Protobuf>` entry to `OneMoreTaskTracker.Tasks.csproj`. `Task.cs`: changed `public required int FeatureId { get; init; }` → `public int FeatureId { get; private set; }` and added `AttachToFeature(int)` guarding `featureId <= 0` with `ArgumentOutOfRangeException` (spec §§148–161). Dropped `required` per spec §153; `CreateTaskHandler.Create` switched to `task.AttachToFeature(request.FeatureId)` after object-initializer construction (private setter is class-private, not assembly-internal). New handler `Tasks/Attach/AttachTaskToFeatureHandler.cs` implementing both `Attach` (validates, NotFound on missing task, idempotent when already attached — guard-checks `task.FeatureId != request.FeatureId` before `AttachToFeature` + `SaveChangesAsync`) and `Detach` (throws `FailedPrecondition` when `reassign_to_feature_id <= 0`, otherwise delegates to `Attach`). Response `TaskState` cast via `(Proto.Tasks.TaskState)task.State`. Wired single `app.MapGrpcService<AttachTaskToFeatureHandler>()` in `Program.cs`. Added `tests/.../TestHelpers/TaskTestExtensions.cs` with `WithFeature(int)` chainable extension. Updated 4 existing test files (`TaskAddMrTests`, `TaskAddProjectTests`, `ListTasksHandlerTests`, `GetAssigneeTaskSummaryHandlerTests`) replacing `new Task { ..., FeatureId = 1 }` initializer (now inaccessible due to private setter from test assembly) with `new Task { ... }.WithFeature(1)`. New `AttachTaskToFeatureHandlerTests.cs` with 6 facts: unknown jira_task_id → NotFound, feature_id=0 → InvalidArgument, idempotent when already attached (asserts `db.ChangeTracker.HasChanges() == false` after handler call), happy path attach moves FeatureId + response carries new id, Detach without reassign → FailedPrecondition, Detach with valid reassign → happy. Grep for `FeatureId` assignment callers: 1 production site (`CreateTaskHandler` — updated to `AttachToFeature`) + 4 test files (updated via `.WithFeature(1)`). Build: clean (0 warn, 0 err on Tasks project; full solution 11 pre-existing unrelated warnings). Tests: 59/59 Tasks pass, 299/299 total across all projects. | pending |
| 2 | Specs 03+04 — Feature write + read handlers. Added `Mapster 7.4.0` to `OneMoreTaskTracker.Features.csproj`. Created `Features/Data/FeatureMappingConfig.cs` registering 4 separate `TypeAdapterConfig<Feature, FeatureDto>` — one per proto namespace (Create/Update/List/Get each declare their own DTO). State cast is `(ProtoFeatureState)s.State` with **no +1 offset** (confirmed by reading generated `obj/Debug/net10.0/FeatureState.cs` — no Unspecified member, ordinals shared). Description: null → empty string. Dates: null → empty string else `yyyy-MM-dd`. Timestamps: `ToString("O")`. Created `Features/Data/FeatureValidation.cs` with shared `ParseOptionalDate` + `ValidateDateOrder`. Rewrote all 4 handlers as primary-constructor `(FeaturesDbContext db)` classes implementing spec 03/04 semantics (trim+null-empty title/description; default `LeadUserId = ManagerUserId`; `Enum.IsDefined` guard on update state; NotFound on missing id; AsNoTracking on reads). `ListFeaturesHandler` uses composite nulls-last ordering `(PlannedStart == null, PlannedStart, Id)` — avoids the non-translatable `DateOnly.MaxValue` on Npgsql; window semantics exactly per spec 04 §61–65 (unscheduled always included). `Program.cs` calls `FeatureMappingConfig.Register()` after `AddGrpc()`. Updated `HandlerRegistrationTests.cs` (stubs were asserting Unimplemented; now assert real error paths since handlers need a `FeaturesDbContext` ctor arg). Added `CreateFeatureHandlerTests`, `UpdateFeatureHandlerTests`, `ListFeaturesHandlerTests`, `GetFeatureHandlerTests` — each uses fresh InMemory DB per test + calls `FeatureMappingConfig.Register()` from ctor. Build: clean (0 warn/0 err). Tests: 27/27. | pending |
| 5 | Spec 08 — Gantt Storybook foundations + fixtures. New `src/features/gantt/stateConfig.ts` mirroring `features/team/stateConfig.ts` style with **raw token names** (`--state-not-started`, …) not wrapped `var(--…)` — deliberate deviation from spec §§36–44 to match codebase convention. Exports `FeatureStateEntry`, `FEATURE_STATE_ENTRIES` (5 rows: CsApproving→`--state-not-started`, Development→`--state-in-dev`, Testing→`--state-in-test`, EthalonTesting→`--state-mr-master`, LiveRelease→`--state-completed`), `FEATURE_STATE_CSS` (Object.fromEntries). New `src/features/gantt/__fixtures__/FeatureFixtures.ts` with frozen literals — `FIXTURE_TODAY='2026-04-21'`, 4 `MiniTeamMember` consts (qa/fe/be/mg with userIds 10/11/12/1), 5 `FeatureSummary` exports (SOLO id=101, MINI_TEAM id=102, UNSCHEDULED id=103 with null dates + empty taskIds, OVERDUE id=104 plannedEnd=2026-04-10, SHIPPED id=105 LiveRelease), `ALL_FEATURES` in spec §152 order `[OVERDUE, SHIPPED, SOLO, MINI_TEAM, UNSCHEDULED]`, plus `MINI_TEAM_FEATURE_DETAIL` wrapping 5 `AttachedTask` rows across be/fe/qa. New `src/features/gantt/__fixtures__/FeatureFixtures.mdx` (title `Plan/Foundations/Fixtures`) — pure MDX table listing each fixture's id/state/dates/taskCount via `{FIXTURE.field}` interpolation + second table for `MINI_TEAM_FEATURE_DETAIL` tasks. **Deviation**: spec §16 specifies `.stories.mdx` extension, but Storybook 10's CSF indexer rejects docs-only MDX with `.stories.mdx` (`No matching indexer found`) — renamed to plain `.mdx` to match existing `src/shared/ds/foundations/*.mdx` convention; story registers correctly as `plan-foundations-fixtures--docs` in `storybook-static/index.json`. `.storybook/preview.ts` rewritten: imports existing `i18n` instance from `../src/i18n/config` (same instance app uses, preserves namespace config `common/header/auth/tasks/team/gantt`), adds `decorators: [withI18n]` wrapping stories in `<I18nextProvider>`, sets `document.documentElement.dataset.theme = 'dark'` via `useEffect`, `useEffect` on locale globals calls `i18n.changeLanguage(locale)`; new `globalTypes.locale` toolbar with ru/en items (default ru, flag emojis in `right`, `dynamicTitle: true`), appended `'Plan'` to `parameters.options.storySort.order`. `.storybook/main.ts` unchanged — `**/*.mdx` already in stories glob. No new CSS tokens introduced. No React components created. Build: clean (231 modules, no TS errors). Tests: 215/215 across 27 files. Storybook build: success (MDX indexed). Verify path: `storybook-static/index.html` → Plan/Foundations/Fixtures. | pending |
| 4 | Spec 07 — Plan gateway REST (PlanController). New `OneMoreTaskTracker.Api/Controllers/PlanController.cs` with 6 endpoints (list/get/create/update/attach/detach) plus DTOs (`FeatureSummaryResponse`, `FeatureDetailResponse`, `AttachedTaskResponse`, `MiniTeamMemberResponse`, `CreateFeaturePayload`, `UpdateFeaturePayload`, `DetachTaskBody`) inlined at file bottom mirroring `TasksController` style. Controller hosts 4 `MapSummary` overloads (one per proto-namespaced `FeatureDto`: Create/Update/List/Get) delegating to a shared `BuildSummary`, plus an inline `MapState` switch matching spec §§177–179. `CreateFeature`: `managerUserId = callerId` always; `leadUserId = body.LeadUserId > 0 ? value : callerId`. `AttachTask` pre-checks via `featureGetter.GetAsync(id)` so NotFound propagates to 404 before any Tasks-service hop. `DetachTask` rejects `body == null || ReassignToFeatureId <= 0` directly as 422 in-controller. `MiniTeam` resolution reuses `UserService.GetTeamRoster(managerId=feature.ManagerUserId)` since `UserService` has no `GetUsersByIds` RPC (matches `TeamController` pattern); `ExtractDisplayName` duplicated from `TeamController` (pulling to shared helper was out of scope). Added 6 `<Protobuf>` entries to `OneMoreTaskTracker.Api.csproj` (Features: state enum + 4 per-use-case + Tasks `attach_task_command_handler.proto`), `<GrpcServices>Client</GrpcServices>`. `Program.cs` registers `FeaturesService:Address` config key + 5 new `AddGrpcClient<>`: 4 Features clients + `TaskFeatureLinker`. Added `FeaturesService:Address = http://localhost:5110` to `appsettings.json` (no `appsettings.Development.json` in repo). `GrpcExceptionMiddleware` gained `StatusCode.FailedPrecondition => (422, "Precondition failed")`. `TasksController.CreateTaskPayload` / `CreateTask` were already extended to `FeatureId` in an earlier iteration — left unchanged. Tests: new `PlanControllerTests.cs` (8 facts) covers 401-anon, 403-Qa, CreateFeature with `managerUserId` overridden by callerId + LeadUserId fallback, AttachTask NotFound → 404, DetachTask missing-body/zero-id → 422, DetachTask happy → 200, ListFeatures happy. New row in `GrpcExceptionMiddlewareTests` theory covers `FailedPrecondition` → 422. `ApiWebApplicationFactory`, `TasksControllerWebApplicationFactory`, `ProductionWiringWebApplicationFactory`, `ProgramDiTests` all updated to register/resolve 5 new client types. **Deviation**: `TaskDto` in `ListTasksResponse` has no `feature_id` field, so per-feature `taskCount` / `taskIds` cannot be computed in `ListFeatures` or `GetFeature` — spec §112 says "filter in-memory if proto doesn't support it" but the field is absent entirely, so current implementation returns `taskCount=0`, `taskIds=[]`, `tasks=[]` with explanatory inline comments flagging the missing proto field (spec does not authorize modifying Tasks protos in this spec). **Open questions**: (1) should `ListTasksRequest` proto grow a `feature_id` filter in a follow-up spec to unblock task-count composition? (2) `UpdateFeaturePayload` has no mask/sparse semantics over gRPC — current code forwards empty strings for untouched fields, relying on the Features handler's trim-and-null logic; may over-clear fields the client didn't intend to change. Build: clean (0 warn / 0 err on Api project; 13 pre-existing unrelated warnings on other test projects). Tests: 127/127 Api, 308/308 solution-wide. | pending |
| 6 | Specs 09+10+11 — Gantt primitives (layout hook + timeline), feature row + assignee stack, feature drawer. Added `ganttMath.ts` (pure `parseIsoDate`/`toIsoDate`/`addDays`/`daysBetween`/`mondayOf`/`windowForZoom`/`barGeometry`/`todayPercent`) + `useGanttLayout.ts` (useMemo wrapper returning `{window, lanes, unscheduled, todayPercent}`). Added `GanttTimeline.tsx` + `.css` (7-col grid per day, week-start rule, today hairline, day labels). Added `GanttAssigneeStack.tsx` + `.css` (overlapping `Avatar`s from `shared/ds`, +k `Badge`, solo label, placeholder circle). Added `GanttFeatureRow.tsx` + `.css` (sticky gutter with title/lead/stack, absolute bar with state tint via `FEATURE_STATE_CSS`, clamp carets, reveal-tasks sub-bars using `STATE_CLASS`, overdue underline). Added `FeatureDrawer.tsx` + `.css` + `useFeatureDrawerData.ts` + `FeatureEditForm.tsx` (right-anchored aside with role=dialog/aria-modal, Escape closes, slide-in animation; sparse PATCH via buildPatch diff; attach form + inline reassignment picker for detach; canEdit gates Edit/Attach/Detach). Stories: `Plan/Primitives/GanttTimeline` (4), `Plan/Primitives/GanttAssigneeStack` (5), `Plan/Primitives/GanttFeatureRow` (6), `Plan/Composed/FeatureDrawer` (8) — all consume `FIXTURE_TODAY`, no `new Date()`. i18n additions in both en/ru: `gantt:drawer.detachTarget`, `gantt:drawer.validation.titleRequired`. **Deviations**: (1) project uses plain `.css` with BEM not CSS modules — all four CSS files follow `.gantt-timeline__day`/`.gantt-row__bar`/`.feature-drawer__body` convention (spec explicitly allowed this fallback). (2) Spec 09 test "Today before window.start → null" is logically unreachable because `windowForZoom` is Monday-anchored on today itself; replaced with the positive assertion that `todayPercent` lies inside `[0,100)` by construction. (3) `FeatureEditForm`'s `LeadPicker` subcomponent is deferred — no `leadUserId` editing in this pass (out of scope for primitives; drawer does not expose Lead in edit form; `CreateFeature` / team-roster lookup already covered elsewhere). No test for `FeatureEditForm.test.tsx` as a standalone file — its behavior is covered via `FeatureDrawer.test.tsx` (sparse-patch / clean-save-disabled). Tests: 253/253 (up from 215, +38 new). Storybook: 23 new stories. Build: clean (TS 0 errors, vite 639ms). Storybook build: success. **Open questions**: (1) drawer detach reassignment needs a features list fetch (not wired — `reassignOptions` is an empty state; real UX needs `listFeatures()` on demand). (2) `STATE_CLASS` in row sub-bars uses task state tokens via CSS class name convention — verified against existing `src/styles/primitives/*` not directly but class names match `.gantt-row__task-bar--in-dev` style referenced by tokens in the new `GanttFeatureRow.css`. | pending |
| 7 | Specs 12 + 13 combined — Gantt page assembly + routing + role-aware home redirect. New files under `src/features/gantt/`: `GanttToolbar.tsx` + `.css` + 4 stories (`ManagerDefault`/`NonManager`/`MonthZoom`/`FiltersApplied`), `GanttEmpty.tsx` + `.css` + 2 stories (`WithCTA`/`WithoutCTA`), `CreateFeatureDialog.tsx` + 3 stories (`Open`/`Submitting`/`ValidationError`), `GanttPage.tsx` + `.css` + `.test.tsx` (8 tests) + 7 stories (`Populated`/`PopulatedQa`/`EmptyManager`/`EmptyQa`/`LoadingState`/`ErrorState`/`DrawerOpen`). Prior-wave files already present and unchanged: `useGanttPageState.{ts,test.ts}` (11 tests), `usePlanFeatures.ts`, `useTeamRoster.ts`. Routing: new `src/app/HomeRoute.tsx` (role-aware `<Navigate replace>` to `/plan` for Manager else `/tasks`, falls back to `/login` when unauthenticated — even though always mounted inside `ProtectedRoute`, defensive fallback avoids tsc non-null assertion) + `HomeRoute.test.tsx` (5 tests: Manager→/plan, FE/BE/Qa→/tasks, unauth→/login). `src/app/App/App.tsx` rewritten: `/` now wraps `<HomeRoute>` in `ProtectedRoute`, added `/tasks` → `TaskPage`, added `/plan` → `GanttPage`; kept `/tasks/:jiraId` and `/team` intact. `AppHeader.tsx` rewritten: replaced single Tasks/Team nav with Plan (all roles) + Tasks (non-Managers only) + Team (preserved unconditionally, matching existing behavior per spec §68 verification note); active highlight uses existing `app-header__nav-item--active` classname, derived from `useLocation()`; `<a>` tags kept (no `NavLink` swap) to mirror the pre-existing pattern the spec allows. `AppHeader.test.tsx` rewritten with 3 role-scoped describe blocks (Manager: Plan+Team visible, Tasks hidden, active highlight on /plan; FrontendDeveloper: Plan+Tasks visible + active highlight on /tasks; Qa: Plan+Tasks visible). **Deviations**: (1) project uses plain `.css` + BEM (not `.module.css` as spec 12 §13 suggests) — all three new CSS files follow `.gantt-toolbar__…` / `.gantt-empty__…` / `.gantt-page__…` convention (matches `TeamPage.css`/`GanttFeatureRow.css` etc.). (2) DS `Button` has no `subtle` variant (spec 12 §72) — used `ghost` instead with `aria-pressed` + `--active` classname suffix for the pressed visual. (3) Test seam pattern: `GanttPage.tsx` exports BOTH `GanttPageInternal` (pure presentational — takes `features`/`roster`/`loading`/`error` via props, used by tests + stories) AND `GanttPage` (real wrapper that does I/O via `useAuth`/`usePlanFeatures`/`useTeamRoster` and routes into `GanttPageInternal`). Stories render `GanttPageInternal` wrapped in `MemoryRouter` to avoid real fetch; tests use the same seam with `vi.setSystemTime(FIXTURE_TODAY)` to pin the layout window deterministically. (4) `CreateFeatureDialog` uses a bespoke in-dialog `<form>` with blank defaults rather than reusing `FeatureEditForm` directly — `FeatureEditForm` is keyed to a concrete `FeatureSummary` and produces a sparse `UpdateFeaturePayload`, which doesn't cleanly match the `CreateFeaturePayload` shape; forcing reuse would require a bigger refactor of FeatureEditForm's internal diffing. The dialog shares styling classes (`.feature-drawer__form`/`.feature-drawer__grid`/`.feature-drawer__actions`) with the drawer form for visual consistency. (5) DS `Dialog` does NOT implement a focus trap (verified by reading `shared/ds/Dialog/Dialog.tsx` — focuses container once on open, Esc closes, but does not cycle focus inside). Spec 12 §98 acknowledged this limitation — we still use it for `CreateFeatureDialog` as instructed. (6) Lazy task sub-bars: hover wired via `state.revealTasks(lane.feature.id)` + `state.revealedFeatureId`; actual `planApi.getFeature(id)` fetch for per-row tasks NOT wired in v1 — `GanttFeatureRow` receives `tasks={undefined}` so its reveal state renders the existing `gantt-row__task-skeleton` placeholder. Spec 12 §§170–172 explicitly permits deferring the fetch. (7) Mini-team resolution: spec §168 calls for resolving `taskIds → assignees` by joining with the roster, but `FeatureSummary` only carries `taskIds: number[]` (no per-task `userId` until the FeatureDetail lazy fetch lands), so `miniTeamFor()` currently returns `[lead]` only — feature detail drawer still shows the full mini-team from `FeatureDetail.miniTeam`. Documented as a known degradation. (8) `AppHeader.test.tsx` uses Russian strings (`План`/`Задачи`/`Команда`/`Выйти`) because the existing i18n default is `ru` and the previous header test also used RU — matches prevailing convention. **Design Notes**: code-only eval mode — skipped browser-based visual regression (no 1280/1440/1920 snapshots taken). What was checked: (a) hierarchy ordering `Plan → Tasks → Team` in AppHeader; (b) active-class parity with existing nav pattern; (c) `--day-count` inline custom property flips with zoom (asserted in `GanttPage.test.tsx` via `main.style.getPropertyValue('--day-count')` going `14 → 30` on Month click); (d) sticky toolbar + overflow-auto timeline-wrap for proper horizontal scroll; (e) drawer slide-in already confirmed in prior wave via `transform: translateX(…)` (not `right`/`width`). What was skipped: day-tick density at week zoom, `mini-team of N` wrapping at 280px gutter, today-hairline contrast vs the 5 state colors — all require visual inspection that code-only eval can't do. **Known degradations**: (a) lazy task sub-bars render skeleton only; (b) mini-team on timeline = lead only until drawer opens; (c) no axe-core run on new stories in this wave; (d) `CreateFeatureDialog` lacks a Lead picker — uses caller-as-lead fallback semantics on the backend. Build: clean (tsc 0 errors; vite 677ms, no new warnings). Tests: 281/281 across 34 files (+17 new: 8 GanttPage, 5 HomeRoute, 4 new AppHeader cases — AppHeader test count net: was 4, now 8). Storybook build: success (16 new stories registered: 4 Toolbar + 2 Empty + 3 CreateDialog + 7 Page). Commit: pending. |
