{
  "$comment": "Project-local override authored by gan-feature-tech-planner. Canonical runners.json under ~/.claude/scripts/gan-feature/runners.json has all fields null; this file pins the actual commands for OneMoreTaskTracker. Backend uses dotnet against the slnx solution; frontend uses vitest run via the npm test script (TAP reporter for the harness).",
  "frontend": {
    "lint": "npm --prefix OneMoreTaskTracker.WebClient run lint",
    "typecheck": "npx --prefix OneMoreTaskTracker.WebClient tsc -p OneMoreTaskTracker.WebClient/tsconfig.json --noEmit",
    "test": "npm --prefix OneMoreTaskTracker.WebClient test -- --reporter=tap",
    "build": "npm --prefix OneMoreTaskTracker.WebClient run build",
    "e2e": "npm --prefix OneMoreTaskTracker.WebClient run e2e"
  },
  "backend": {
    "lint": null,
    "typecheck": "dotnet build OneMoreTaskTracker.slnx -c Debug --nologo",
    "test": "dotnet test OneMoreTaskTracker.slnx --nologo --logger:\"console;verbosity=normal\"",
    "build": "dotnet build OneMoreTaskTracker.slnx -c Debug --nologo",
    "migrate": "dotnet ef database update --project OneMoreTaskTracker.Features"
  }
}
